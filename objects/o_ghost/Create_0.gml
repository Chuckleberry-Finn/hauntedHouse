o_z = -110

// Set the circle’s center position
center_x = x; // or any x-coordinate in the room
center_y = y; // or any y-coordinate in the room

// Set the radius of the circle path
radius = irandom_range(50, 200); // Random radius between 50 and 100

// Set an initial angle for the movement
angle = irandom_range(0, 360); // Start at a random angle in degrees

// Set the rotation speed (adjust to control speed)
rotation_speed = random_range(1, 3); // Random speed between 1 and 3