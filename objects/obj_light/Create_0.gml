range = 200;  // Range of light (how far it reaches)
intensity = 40.0;  // Intensity of light (controls how bright the light is)
color = c_red;  // Light color (you can adjust this later for colored lights)