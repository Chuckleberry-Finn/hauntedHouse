range = 100;  // Range of light (how far it reaches)
intensity = 255.0;  // Intensity of light (controls how bright the light is)
color = c_white;  // Light color (you can adjust this later for colored lights)